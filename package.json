{
  "name": "kenpali",
  "version": "0.17.0",
  "description": "A minimalistic programming language for scripting applications",
  "main": "index.js",
  "types": "index.d.ts",
  "type": "module",
  "scripts": {
    "test": "tsc -p tsconfig.json && ava",
    "benchmark": "node test/benchmark.js",
    "reset-benchmark": "rm src/baseline/* && rm src/previous/* & cp src/*.* src/baseline && cp src/*.* src/previous",
    "bootstrap": "node src/kp.js compile kenpali.kpc --use compiler.kpc --use parser.kpc"
  },
  "author": "Graham Hill",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/def-gthill/kenpali-js"
  },
  "devDependencies": {
    "@ava/typescript": "^6.0.0",
    "ava": "^6.4.1",
    "eslint": "^8.41.0",
    "prettier": "^3.5.3",
    "tsd": "^0.33.0",
    "typescript": "^5.9.3"
  },
  "ava": {
    "files": [
      "test/**/*.test.*"
    ],
    "typescript": {
      "rewritePaths": {
        "test/": "test-build/"
      },
      "compile": false
    }
  },
  "tsd": {
    "directory": "test"
  }
}
